# @knowledgeos/api

Node HTTP API for KnowledgeOS.

This app validates external requests with Zod, delegates ingestion and retrieval to packages, and keeps LLM/database details out of route handlers.

