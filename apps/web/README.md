# @knowledgeos/web

Next.js App Router dashboard and HTTP API for KnowledgeOS.

Milestone one includes Markdown ingestion, concept browsing, and semantic search. Browser code talks to local `/api/*` route handlers through `@knowledgeos/sdk`; only server-side route handlers access the database and embedding provider.
