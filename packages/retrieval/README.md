# @knowledgeos/retrieval

Retrieval and embedding package.

Milestone one implements provider-injected embeddings and pgvector semantic search. The result shape already includes explanations and citations so hybrid retrieval can be added without changing consumers.

