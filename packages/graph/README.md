# @knowledgeos/graph

Knowledge graph package placeholder.

This package will own the derived Neo4j graph index in a later milestone. PostgreSQL remains the system of record, and the graph should always be rebuildable.

