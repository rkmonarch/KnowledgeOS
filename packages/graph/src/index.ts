export interface GraphIndexStatus {
  enabled: false;
  reason: "deferred_until_graph_milestone";
}

export const graphIndexStatus: GraphIndexStatus = {
  enabled: false,
  reason: "deferred_until_graph_milestone"
};

