# @knowledgeos/shared

Shared domain primitives for KnowledgeOS.

This package owns stable types, Zod schemas, branded identifiers, environment validation, hashing, errors, and logging interfaces. It should not depend on application packages or infrastructure packages.

