# @knowledgeos/sdk

Typed API client for KnowledgeOS.

The dashboard consumes this package instead of manually calling `fetch`. Future external consumers can use the same request and response contracts.

