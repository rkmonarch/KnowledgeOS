# @knowledgeos/okf

Open Knowledge Format export package placeholder.

This package will translate internal concepts into OKF-compatible Markdown with YAML front matter. It is intentionally separate from the database model so future OKF specification changes do not force storage changes.

